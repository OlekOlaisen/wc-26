# Leave unset in local dev — the app uses /api via the Vite proxy (no CORS issues).
# For production builds, set the full API URL:
# VITE_API_BASE_URL=https://worldcup26.ir
#
# Optional JWT if the API requires authentication:
# VITE_API_TOKEN=your_token_here
